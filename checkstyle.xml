<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE module PUBLIC
        "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
        "https://checkstyle.org/dtds/configuration_1_3.dtd">

<module name="Checker">

    <!-- Impostazioni generali -->
    <property name="charset" value="UTF-8"/>
    <property name="fileExtensions" value="java"/>
    <property name="tabWidth" value="4"/>
    <property name="severity" value="warning"/>

    <!-- Controllo lunghezza righe -->
    <module name="LineLength">
        <property name="max" value="150"/>
        <property name="ignorePattern"
                  value="^package.*|^import.*|href\s*=|http://|https://"/>
    </module>

    <!-- TreeWalker: regole principali sul codice -->
    <module name="TreeWalker">

        <!-- Import -->
        <module name="AvoidStarImport"/>
        <module name="UnusedImports"/>

        <!-- Nomi coerenti -->
        <module name="PackageName"/>
        <module name="TypeName"/>
        <module name="MethodName"/>
        <module name="ParameterName"/>
        <module name="MemberName"/>
        <module name="ConstantName"/>

        <!-- Spazi e formattazione -->
        <module name="WhitespaceAround"/>
        <module name="WhitespaceAfter"/>
        <module name="NoWhitespaceBefore"/>

        <!-- Struttura del codice -->
        <module name="NeedBraces"/> <!-- Impone le graffe per if/for ecc. -->
        <module name="EmptyBlock"/>
        <module name="RedundantModifier"/>
        <module name="ModifierOrder"/>

        <!-- Complessità/metodi troppo lunghi -->
        <module name="MethodLength">
            <property name="max" value="100"/>
        </module>
        <module name="CyclomaticComplexity">
            <property name="max" value="10"/>
        </module>

    </module>

    <!-- Assicura newline finale nei file -->
    <module name="NewlineAtEndOfFile"/>

</module>